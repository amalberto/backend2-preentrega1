<div class="container admin-panel">
    <h1>🛠 Panel de Administración</h1>
    <h2>Bienvenido, {{user.first_name}}</h2>

    <!-- Tabs de navegación -->
    <div class="admin-tabs">
        <button class="tab-btn active" data-tab="mail">📧 Mail</button>
        <button class="tab-btn" data-tab="tickets">🎫 Tickets</button>
        <button class="tab-btn" data-tab="carts">🛒 Carritos</button>
    </div>

    <!-- Tab: Mail -->
    <div class="admin-section tab-content" id="tab-mail">
        <h3>📧 Configuración de Mail</h3>
        
        <div class="mail-checks">
            <button id="checkConfig" class="btn btn-secondary">Verificar Config</button>
            <button id="checkSmtp" class="btn btn-secondary">Verificar SMTP</button>
            <span id="configStatus" class="status"></span>
        </div>

        <div class="mail-test">
            <h4>Enviar Email de Prueba</h4>
            <form id="testMailForm">
                <div class="form-group">
                    <label for="testEmail">Email destino:</label>
                    <input type="email" id="testEmail" name="to" required placeholder="test@ejemplo.com">
                </div>
                <button type="submit" class="btn btn-primary">Enviar Test</button>
            </form>
            <div id="mailResult" class="result"></div>
        </div>
    </div>

    <!-- Tab: Tickets -->
    <div class="admin-section tab-content hidden" id="tab-tickets">
        <h3>🎫 Tickets de Compra</h3>
        <div class="filter-section">
            <input type="text" id="ticketFilter" placeholder="Filtrar por email..." class="filter-input">
        </div>
        <div id="ticketsList" class="admin-list">
            <p class="loading">Cargando tickets...</p>
        </div>
    </div>

    <!-- Tab: Carritos -->
    <div class="admin-section tab-content hidden" id="tab-carts">
        <h3>🛒 Carritos Activos</h3>
        <div class="filter-section">
            <input type="text" id="cartFilter" placeholder="Filtrar por email..." class="filter-input">
        </div>
        <div id="cartsList" class="admin-list">
            <p class="loading">Cargando carritos...</p>
        </div>
    </div>

    <div class="admin-actions">
        <a href="/admin-products" class="btn btn-warning">📦 Gestionar Productos</a>
        <a href="/users/current" class="btn btn-secondary">← Volver al Perfil</a>
        <a href="/products" class="btn btn-primary">🛒 Ver Productos</a>
    </div>

    <form action="/api/users/logout" method="POST" style="margin-top: 20px;">
        <button type="submit" class="logout-btn">Cerrar Sesión</button>
    </form>
</div>

<script src="/js/adminPanel.js"></script>
